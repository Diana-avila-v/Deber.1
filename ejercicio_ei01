Algoritmo digitos
	Escribir "Escribe un número:"
	leer n
	contador <- 0
	Mientras n >= 1 Hacer
		n = trunc(n/10)
		contador <- contador + 1
	FinMientras
	Escribir "El número que ingresó tiene ", contador, " digitos"
FinAlgoritmo
